<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>Starter Template</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="">
<meta name="author" content="">

<!-- styles -->
<style> body {padding-top: 60px; } </style>
     <link rel="stylesheet" type="text/css" href="{{=URL('static','css/jquery.js')}}">
     <link rel="stylesheet" type="text/css" href="{{=URL('static','css/bootstrap.min.css')}}">
     <link rel="stylesheet" type="text/css" href="{{=URL('static','css/bootstrap-responsive.min.css')}}">
    <link rel="stylesheet" type="text/css" href="{{=URL('static','css/css/web2py_bootstrap.css')}}">

<!-- scripts -->

</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top" style="background-color:black" role="navigation">
   <ul class="nav navbar-nav">
       <li>{{=A('Home', _href=URL('index'))}}</li>
        <li>{{=A('New post', _href=URL('new'))}}</li>
   </ul>
</div>
<div class="container">
    <div class="row">
        <div class="span10">
            {{include}}
        </div>
        <div class="span2">
            {{if not auth.user_id:}}
                <span class="label label-warning">Not logged in? {{=A('login', _href=URL('/user/login'))}}</span>
            {{pass}}
            {{if auth.user_id:}}
                <span class="label label-success">Welcome {{=auth.user.first_name}}</span>
                <div class="btn-group">
                <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">Account<span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <!-- dropdown menu links -->
                    <li><a>Profile</a></li>
                    <li><a>Messages</a></li>
                    <li>{{=A('Logout',_href=URL('default/user','logout'))}}</li>
                </ul>
                </div>
            {{pass}}
        </div>
    </div>
</div> <!-- /container -->
    <script src="{{=URL('static','js/jquery.js')}}"></script>
    <script src="{{=URL('static','js/bootstrap.min.js')}}"></script>
    <script src="{{=URL('static','js/web2py_bootstrap.js')}}"></script>

</body>
</html>
